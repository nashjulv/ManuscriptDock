# Schemas

This directory will contain versioned, machine-readable schemas for:

- canonical manuscript objects;
- submission rule packs;
- manuscript passports;
- immutable snapshots and manifests;
- academic knowledge bodies, independently versioned AI review reports, and relationship assertions;
- PWC exchange packages.

Schemas must be independent from journal-specific UI and include explicit schema versions and migration rules.
