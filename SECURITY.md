# Security Policy

ManuscriptDock 会处理未发表论文、审稿意见、作者身份和机构规则，安全与隐私属于产品功能，而不是发布后的补充项。

## 报告安全问题

在正式安全联系方式公布前，请通过项目维护者提供的私密渠道报告漏洞。不要在公开 Issue 中提交：

- 未发表论文或审稿材料；
- API 密钥、访问令牌和签名密钥；
- 可直接利用的漏洞细节；
- 真实用户目录、日志和数据库。

正式发布前必须在本文件加入可验证的安全邮箱、支持版本和响应策略。

## 安全基线

- 私密稿件默认只保存在本地；
- 所有出网请求通过受控出口；
- 外发前说明内容、接收方、目的和版本；
- 规则包和远程更新必须验证来源与签名；
- 密钥使用操作系统安全存储，不写入仓库或普通配置文件；
- 外部 PDF、网页、模板和规则均按不可信输入处理；
- 不在遥测、崩溃报告和普通日志中记录论文内容；
- 发布和专业评审不能绕过用户确认。
