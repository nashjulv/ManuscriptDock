# V0.622 三档阅读字号

- 原“更小”作为新默认；正文更小 / 默认 / 更大为 13 / 14 / 15px，标题及辅助文字同步调整。
- Aa 浮层保留三个选项。原生偏好 schema 2 与浏览器 v2 键区分新旧字号含义：旧 small 对应新 default，旧 default/large/xlarge 对应新 large；保留旧设置文件，用户选择时写入新格式。
- 版本由 V0.621 增加 0.001 至 V0.622，包元数据同步为 0.622.0。

验证：开发期间 93 项前端回归及 3 项 Rust 偏好测试通过，覆盖切换、字号变量、持久化、旧偏好迁移和错误恢复；应用构建通过。提交前再次验证前端构建与差异空白。按用户要求不启动应用。

Internationalization review：zh-CN / en 的三档标签、键盘操作、保存及错误提示已覆盖。字体、原稿及导出内容的语言保持不变。
