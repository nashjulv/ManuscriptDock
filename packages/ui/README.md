# Shared UI

This package will contain React components and design tokens shared by ManuscriptDock interfaces.

The source of truth is [the design system](../../design-system/manuscriptdock/MASTER.md). Components must support keyboard use, visible focus, reduced motion, sufficient contrast, and both pointer and touch interaction.

No runtime CDN fonts, scripts, or icons are permitted in the desktop application.
