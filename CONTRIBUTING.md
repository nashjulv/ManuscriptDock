# Contributing to ManuscriptDock

ManuscriptDock 当前处于产品设计和工程初始化阶段。本文件定义仓库形成源码以后共同遵循的最低约定。

## 开始之前

1. 先阅读 [产品设计总纲](docs/product-design-overview.md)；
2. 涉及界面时阅读 [设计系统](design-system/manuscriptdock/MASTER.md)；
3. 涉及投稿格式时阅读 [投稿规则系统](docs/submission-rule-system.md)；
4. 涉及数据外发时遵循本地优先、明确授权和不可变快照原则；
5. 不修改标记为历史资料的文件，除非任务明确要求。

## 分支和提交

- 默认分支：`main`；
- 功能分支：`feature/<short-name>`；
- 修复分支：`fix/<short-name>`；
- 文档分支：`docs/<short-name>`；
- 提交应当小而完整，说明结果和原因；
- 不在提交中混入无关格式化或生成物。

建议提交前缀：`feat`、`fix`、`docs`、`refactor`、`test`、`build`、`chore`。

## 代码质量

- 新功能必须包含与风险相称的测试；
- 解析、转换和规则执行优先使用确定性逻辑；
- 自动修改必须可比较、可撤销且不能覆盖原稿；
- UI 不直接访问任意文件系统或外部网络；
- 日志不得包含论文正文、审稿材料、令牌或个人敏感信息；
- 合成测试样例不得包含真实未发表论文。

## Pull Request

Pull Request 应说明：

- 解决的问题；
- 影响范围；
- 验证方式；
- 隐私、数据迁移或外发风险；
- 对规则包或历史快照的兼容性；
- 必要的界面截图或交互说明。

## 工具链

正式应用脚手架建立后，应在仓库中锁定 Node.js、Rust、包管理器和依赖版本，并在本文补充唯一的安装、检查、测试和构建命令。在此之前不要添加无法执行的占位命令。
